import React from "react";
import TreeMap from "../components/TreeMap";

const TreeMapContainer = ({ objProps }) => {
  return (
    <div>
      <TreeMap objProps={objProps} />
    </div>
  );
};

export default TreeMapContainer;
